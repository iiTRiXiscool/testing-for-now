{
  "name": "koxyos-api",
  "version": "1.0.0",
  "description": "Hosting-agnostic backend API + Postgres data layer for the Koxyos barbershop site (and any other shop that reuses the same frontend).",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "migrate": "node scripts/migrate.js",
    "create-shop": "node scripts/create-shop.js",
    "seed-demo": "node scripts/seed-demo.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.12.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "UNLICENSED"
}
